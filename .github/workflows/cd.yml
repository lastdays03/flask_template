name: CD

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to DockerHub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        context: .
        file: docker/Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/flask-template:${{ github.ref_name }}

  # -------------------------------------------------------------------------
  # [선택 사항] 실제 서버 배포 (Deployment)
  # 서버가 준비되면 아래 주석을 해제하고 Secrets를 설정하여 사용하세요.
  # -------------------------------------------------------------------------
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Connect and Deploy via SSH
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         port: 22
  #         script: |
  #           # 프로젝트 폴더로 이동 (서버에 미리 클론/설정 필요)
  #           cd ~/flask_template
  #
  #           # 최신 코드 및 설정 업데이트
  #           git pull origin main
  #
  #           # 최신 Docker 이미지 풀링
  #           docker-compose pull
  #
  #           # 서비스 재시작 (Zero-downtime이 필요하면 Rolling Update 구성 필요)
  #           docker-compose up -d
  #           docker image prune -f
